This folder contains all theme files to theme application

Theme can be applied by 

`<ThemeProvider theme={theme}></ThemeProvider>`

All theme files are split by sections.

Do not use create theme method in theme files, it should be used only in root file `theme.ts`;

Default theme viewer https://mui.com/material-ui/customization/default-theme/
