### What is Store?

It's a Model in the (M)VVM pattern.
Stores should contain all business logic. In a short word, it's the data and business logic of the application.
Stores are use Providers to receive and send data, and they're share data between different parts of application.
